{
  "name": "cp-remote-server",
  "version": "0.1.0",
  "description": "Communication Properly — serveur de salles + jetons Azure",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.0",
    "ws": "^8.18.0"
  }
}
